---
import BaseLayout from '@/layouts/BaseLayout.astro';
import WordplayHeader from '@/components/WordplayHeader.astro';
import LogoAnimation from '@/components/LogoAnimation.astro';
---

<BaseLayout>
  <main>

    <WordplayHeader />

    <!-- ── Center: blog link ── -->
    <div class="blog-cta" id="blog-cta">
      <a href="/blog" id="blog-link" class="blog-link">
        <span class="label">blog</span>
        <svg id="arrow" class="arrow" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <line x1="4" y1="20" x2="20" y2="4" stroke="currentColor" stroke-width="3.5" stroke-linecap="round"/>
          <polyline points="9,4 20,4 20,15" stroke="currentColor" stroke-width="3.5" stroke-linecap="square" stroke-linejoin="miter" stroke-miterlimit="10" fill="none"/>
        </svg>
      </a>
    </div>

    <!-- ── Bottom-right: rotating logo ── -->
    <div class="logo-corner">
      <LogoAnimation instanceId="home" />
    </div>

  </main>
</BaseLayout>

<script>
  import { animate } from 'motion';

  animate(
    '#blog-cta',
    { opacity: [0, 1], y: [12, 0] },
    { duration: 0.8, delay: 0.6, easing: [0.16, 1, 0.3, 1] }
  );

  const blogLink = document.getElementById('blog-link') as HTMLElement;
  const arrow    = document.getElementById('arrow')     as HTMLElement;

  blogLink.addEventListener('mouseenter', () => {
    animate(arrow, { x: 4, y: -4 }, { duration: 0.2, easing: 'ease-out' });
  });

  blogLink.addEventListener('mouseleave', () => {
    animate(arrow, { x: 0, y: 0 }, { duration: 0.2, easing: 'ease-out' });
  });
</script>

<style>
  main {
    height: 100svh;
    display: grid;
    grid-template-rows: auto 1fr auto;
    padding: clamp(2rem, 5vw, 3.5rem);
    position: relative;
  }

  .blog-cta {
    grid-row: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    align-self: center;
    opacity: 0;
  }

  .blog-link {
    display: inline-flex;
    align-items: center;
    gap: 0.25em;
    font-size: clamp(1.1rem, 2vw, 1.4rem);
    font-weight: 800;
    color: var(--black);
    text-decoration: none;
    letter-spacing: -0.01em;
    color: #0047ff;
    text-transform: uppercase;
  }

  .arrow {
    display: block;
    width: 1em;
    height: 1em;
    color: #0047ff;
    flex-shrink: 0;
  }

  .logo-corner {
    position: fixed;
    bottom: clamp(1.5rem, 3vw, 2.5rem);
    right: clamp(1.5rem, 3vw, 2.5rem);
    width: clamp(90px, 11vw, 148px);
    pointer-events: none;
  }

  @media (max-width: 600px) {
    .logo-corner {
      width: 76px;
    }
  }
</style>
